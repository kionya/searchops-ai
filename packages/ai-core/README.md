# packages/ai-core

Optional LLM provider adapters and prompt templates.

This package must stay isolated from the deterministic SEO core loop. No package may require it to run crawl -> analyze -> workorder -> recheck.