# packages/db

Prisma schema, migrations, seed data, and DB client helpers.

This package is the source of truth for persisted data shape. It should not contain SEO rule logic, crawler parsing, or UI code.