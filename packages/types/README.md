# packages/types

Shared DTOs, Zod schemas, enums, and API request/response contracts.

All apps and domain packages should depend on this package for shared contracts instead of duplicating wire shapes.